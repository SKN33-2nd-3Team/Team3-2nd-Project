# 전처리·모델링 상세 결과지 (신규 데이터셋)

작성 2026-07-21. 신규 데이터셋 기준의 전처리 결과서 + 모델 학습 결과서 통합본. 요약본은 `결과지_신규데이터_20260721.md` 참고.

---

## 1. 데이터 및 품질 점검

| 항목 | 값 |
|---|---|
| 학습 데이터 | `data/train.csv` — 125,000행 × 20열, `churned` 포함 |
| 추론 데이터 | `data/test.csv` — 75,000행 × 19열, 라벨 없음 (ID 200000~274999, train과 무중복) |
| 타깃 분포 | 이탈 64,174 (51.34%) / 유지 60,826 (48.66%) — **균형 데이터** |
| 결측값 | train 0건 / test 0건 |
| 중복 행 | train 0건 / test 0건 |
| 특이사항 | `signup_date`는 기준일 대비 일수(음수 정수, −1 ~ −2,922) · 요금제/결제주기/결제수단/상담문의는 균등 분포(합성 정황) |

## 2. 전처리 상세

### 2.1 데이터 분할 (누수 방지의 기준선)

- **Train 75,000 (60%) / Validation 25,000 (20%) / Test 25,000 (20%)**
- `train_test_split(stratify=y, random_state=42)` 2단 분할 — 세 덩어리 모두 이탈률 51.3% 유지
- 원칙: 모든 전처리기 fit과 하이퍼파라미터·threshold 선택은 Train/Validation까지만, **Test는 최종 1회 평가만**

### 2.2 피처 생성 (`src/features.py` MusicFeatureEngineer)

| 처리 | 내용 |
|---|---|
| 제거 | `customer_id` (일련번호), `signup_date` 원본 |
| 파생 1 | `signup_days_ago` = −signup_date (가입 경과 일수, 신규 데이터의 정수형 대응) |
| 파생 2 | `unique_song_ratio` = 고유곡 ÷ (재생곡+1) |
| 파생 3 | `shared_playlist_ratio` = 공유 플레이리스트 ÷ (생성+1) |
| 파생 4 | `hours_per_song` = 청취시간 ÷ (재생곡+1) |
| 파생 5 | `friends_per_playlist` = 친구 수 ÷ (생성 플레이리스트+1) |

(분모 +1은 0으로 나누기 방지. 파생은 Pipeline 내부에서 수행되어 학습·추론 시 동일 적용)

### 2.3 변환 (ColumnTransformer)

| 대상 | 처리 | 수치 |
|---|---|---|
| 수치형 17개 | `SimpleImputer(strategy="median")` | 결측 0건이므로 안전장치 역할 |
| 수치형 (로지스틱 한정) | `StandardScaler` 추가 | 평균 0·표준편차 1 표준화, **Train에만 fit** |
| 범주형 5개 | `SimpleImputer(most_frequent)` → `OneHotEncoder(handle_unknown="ignore")` | location 19 + subscription_type 4 + payment_plan 2 + payment_method 4 + customer_service_inquiries 3 = 32개 더미 |
| **최종 입력 차원** | 수치 17 + 더미 32 = **49개** | |

- 수치형(17): age, num_subscription_pauses, weekly_hours, average_session_length, song_skip_rate, weekly_songs_played, weekly_unique_songs, num_favorite_artists, num_platform_friends, num_playlists_created, num_shared_playlists, notifications_clicked, signup_days_ago + 파생 비율 4종
- 범주형(5): location, subscription_type, payment_plan, payment_method, customer_service_inquiries

### 2.4 파이프라인 구조

```
Pipeline([피처 생성(MusicFeatureEngineer) → 전처리(ColumnTransformer) → 모델])
```

전 과정이 단일 객체로 `artifacts/model/music_churn_pipeline.joblib`에 저장 — Streamlit은 재학습 없이 이 파일만 로드하며, 학습·화면의 전처리가 100% 동일함이 구조적으로 보장된다.

## 3. 사용 모델 전체와 하이퍼파라미터

| 모델 | 하이퍼파라미터 | 스케일링 | 설정 의도 |
|---|---|---|---|
| dummy_prior | strategy="prior" | — | 기준선 (다수 클래스 확률) |
| logistic_raw | max_iter=1200, class_weight="balanced" | O | 해석 가능한 선형 기준, 파생 피처 제외 |
| logistic_engineered | 동일 | O | 파생 피처 효과 비교용 |
| decision_tree | max_depth=8, min_samples_leaf=40, class_weight="balanced" | — | 단일 나무 + 과적합 제동 |
| random_forest | n_estimators=160, max_depth=14, min_samples_leaf=5, class_weight="balanced_subsample" | — | 병렬 다수결 앙상블 |
| extra_trees | n_estimators=160, max_depth=16, min_samples_leaf=4, class_weight="balanced" | — | 무작위성 강화 변형 |
| **gradient_boosting** | **n_estimators=150, learning_rate=0.05, max_depth=3** | — | 순차 오답보완, "천천히·얕게" 규제 |

- 공통: random_state=42 고정 (재현성)
- 하이퍼파라미터는 수동 설정 유지 — 구 데이터에서 50조합 랜덤 탐색으로 현 설정이 최적 평지임을 확인했고, 신규 데이터는 문턱형 신호라 깊이 3 트리로 충분 (로컬 검증: learning_rate 0.1 변경 시에도 PR-AUC 차이 ±0.001)
- class_weight는 균형 데이터(51:49)에서 사실상 무영향 (구조 유지 목적으로 존치)

## 4. 평가 결과 전체

### 4.1 확률 품질 지표 (Validation / Test)

| 모델 | Val PR-AUC | Val ROC-AUC | Val Brier | Test PR-AUC | Test ROC-AUC |
|---|---:|---:|---:|---:|---:|
| **gradient_boosting** | **0.9446** | 0.9391 | 0.1032 | **0.9473** | 0.9416 |
| random_forest | 0.9376 | 0.9321 | 0.1099 | 0.9409 | 0.9354 |
| decision_tree | 0.9259 | 0.9211 | 0.1130 | 0.9314 | 0.9262 |
| extra_trees | 0.9119 | 0.9063 | 0.1297 | 0.9162 | 0.9100 |
| logistic_engineered | 0.8968 | 0.8898 | 0.1354 | 0.9013 | 0.8937 |
| logistic_raw | 0.8968 | 0.8898 | 0.1353 | 0.9014 | 0.8938 |
| dummy_prior | 0.5134 | 0.5000 | 0.2498 | 0.5134 | 0.5000 |

### 4.2 분류 성능 (Test 25,000명)

| 모델 | 기본 0.5: Recall / Precision / F1 | 운영 thr: Recall / Precision / F1 | 비용/고객* |
|---|---|---|---:|
| **gradient_boosting** | 85.6% / 85.0% / 0.853 | **96.2% / 76.8% / 0.854** (thr 0.30) | **0.208** |
| random_forest | 85.1% / 85.5% / 0.853 | 94.4% / 77.2% / 0.849 (thr 0.33) | 0.230 |
| decision_tree | 83.7% / 85.3% / 0.845 | 95.2% / 73.4% / 0.829 (thr 0.23) | 0.251 |
| extra_trees | 81.5% / 83.8% / 0.826 | 94.8% / 71.7% / 0.816 (thr 0.32) | 0.273 |
| logistic 2종 | 80.0% / 81.6% / 0.808 | 94.4% / 69.9% / 0.803 (thr 0.23) | 0.295 |

*FN×3 + FP×1 기준. 선정 모델 Test 혼동행렬: TP 12,343 / FN 492 / FP 3,730 / TN 8,435

### 4.3 선정 모델 피처 중요도 (상위)

청취시간 24.1% > Free 요금제 20.2% > 상담 문의 High 12.8% + Low 12.0% > 일시정지 8.6% > skip률 7.7% > 나이 6.8% > Student 요금제 5.6% (EDA 신호 지도와 완전 일치)

## 5. 운영 threshold 결정 층

- 방법: Validation에서 threshold 0.05~0.95를 0.01 간격 91개 전수 시험, 기대비용(FN×3 + FP×1) 최소점 선택 → **0.30**
- 비용비 민감도 (Validation 실측):

| FN:FP | 최적 thr | 연락 비율 | Recall | Precision | 정확도 |
|---|---:|---:|---:|---:|---:|
| 1:1 | 0.52 | 49.8% | 83.7% | 86.2% | 84.7% |
| 2:1 | 0.36 | 61.4% | 94.4% | 78.9% | 84.1% |
| **3:1 (채택)** | **0.30** | 64.5% | 96.2% | 76.6% | 83.0% |
| 5:1 | 0.28 | 65.2% | 96.5% | 76.0% | 82.6% |
| 8:1 | 0.22 | 70.0% | 97.6% | 71.6% | 78.9% |

- 3:1 근거: 접촉 채널 경제식(비용비 = 방어 성공률×고객 가치 ÷ 접촉 비용)이 주력 채널에서 2.4~8:1 범위이며, 2:1~5:1 구간은 운영점이 거의 동일(강건 구간)해 그 중앙값을 채택. 고비용 채널 운영(1:1)은 Campaign Simulator에서 담당자가 비용·가치 입력으로 선택 가능

## 6. 보조 검증

- **부스팅 대안 비교(로컬)**: XGBoost 0.945 / LightGBM 0.945 — 선정 모델과 동급, 순위 불변
- **규칙 베이스라인**: 위험 깃발 5개(청취≤10h, skip>0.7, 일시정지≥3, 상담High, Free) 합산 점수 PR-AUC 0.81 → ML이 +0.14 우위
- **화면 검증**: Streamlit 6개 페이지 AppTest 전체 통과 + 실서버 구동 확인 (2026-07-21)

## 7. 산출물 목록

| 파일 | 내용 |
|---|---|
| `artifacts/model/music_churn_pipeline.joblib` | 저장 파이프라인 (전처리+모델 일체) |
| `artifacts/model/metadata.json` | 선정 모델·threshold·분할 정보 |
| `artifacts/model_comparison.csv` | 7종 전 지표 비교 |
| `artifacts/threshold_sweep_validation.csv` | threshold 91개 sweep 실측 |
| `artifacts/feature_importance.csv` | 인코딩 피처 중요도 |
| `artifacts/test_predictions.csv` | 제공 test 75,000명 확률·판정 |
| `artifacts/eda/` (23장) + `eda_summary.json` | EDA 차트·요약 |
| `artifacts/model_report.md`, `run_metadata.json` | 실행 보고·데이터 해시 |

## 8. 한계

1. 규칙 기반 합성 데이터 정황(균등 범주 분포, 정수 문턱값 5/10/40h·0.7·3회·35/60세, 위험 깃발 5개 시 이탈률 100%) — 성능 0.947은 생성 규칙 복원 능력이며 실제 서비스 일반화 성능을 보장하지 않음
2. 관측 기준일·해지일 부재 — `churned`의 시간적 정의(향후 N일 내 해지 여부) 미검증
3. 확률 보정(calibration) 곡선은 신규 데이터 기준 미점검 (Brier 0.103으로 양호 추정, 후속 확인 권장)
4. 비용비 3:1은 채널 경제식 기반 가정값 — 실제 채널 비용·성공률 확보 시 재산정 필요
