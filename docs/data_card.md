# Data Card

> 가이드 §②(데이터 수집과 구조 설계)의 필수 기록 7항목.
> 최종 갱신 2026-07-21. 컬럼 정의는 [data_dictionary.md](data_dictionary.md) 참조.

## ⚠️ 미기재 항목 — 발표 전 팀 확인 필요

신규 데이터셋으로 교체(커밋 `2effdcd`)하면서 아래 3개가 기록되지 않았다. **가이드 §② 필수 기록 항목이므로 반드시 채워야 한다.**

| 항목 | 상태 |
|---|---|
| **정확한 출처 URL** | ❌ 미기재 — 누가 어디서 받았는지 기록 없음 |
| **다운로드 날짜** | ❌ 미기재 (파일 타임스탬프상 2026-07-21 추정이나 확증 없음) |
| **라이선스** | ❌ 미기재 |

> 구 데이터셋(41,000행)의 출처는 [Music Streaming Customer Churn Dataset — Kaggle](https://www.kaggle.com/datasets/daliado98/music-streaming-customer-churn-dataset)이었다. 신규 데이터셋은 컬럼 구성이 유사하나 **행 수·값 분포·요금제 수준이 모두 다르므로 동일 출처로 간주할 수 없다.** 확인 없이 위 URL을 신규 데이터 출처로 기재하지 말 것.

## 필수 기록 항목

| 항목 | 내용 |
|---|---|
| **출처** | ⚠️ 위 표 참조 (미기재) |
| **데이터 단위** | 고객 1명 = 1행. `customer_id` 고유값 = 행 수로 검증 완료 |
| **키** | `customer_id`. train 1 ~ 125,000 / test 200,000 ~ 274,999, **교집합 0건** |
| **Target** | `churned` — 데이터셋이 **기존 컬럼으로 제공**. 팀이 생성한 파생 라벨이 아님 |
| **실제 / 합성** | 🔴 **규칙 기반 합성으로 판정** (아래 근거 참조) |
| **개인정보** | 없음. 이름·연락처·이메일·정밀 위치 컬럼 부재. `location`은 미국 주(州) 단위로 식별 위험 낮음 |
| **규모** | train 125,000 × 20 (14.0 MB) / test 75,000 × 19 (8.5 MB). 메모리 약 40 MB |

## 파일 버전 (SHA-256)

| 파일 | 행 수 | SHA-256 |
|---|---:|---|
| `data/train.csv` | 125,000 | `e967211a589021bda2e024c28f31ca84662f53f8c0f2985b565f336bb040b9b5` |
| `data/test.csv` | 75,000 | `b9081ab39f51a65eb39240e9d8c2dc90011efdb554d8b62896d4fa021252a71f` |

`python -m src.run_pipeline` 실행 시 [`artifacts/model/metadata.json`](../artifacts/model/metadata.json)에 자동 기록된다.

## 합성 데이터 판정 근거

가이드는 "합성 데이터는 생성 규칙과 실제 적용 한계를 기록한다"고 요구한다. 관측된 증거는 다음과 같다.

1. **범주 균등성** — 요금제 4종이 각 25.0%, 결제수단 4종 각 25.0%, 상담등급 3종 각 33.3%로 편차 1% 미만
2. **피처 독립성** — 수치형 13개 전체 쌍에서 최대 |상관| = **0.007**. 실제 로그라면 청취시간과 재생곡 수는 강한 양의 상관이어야 한다
3. **논리 위반** — 고유곡 > 총재생곡 29.6%, 공유 > 생성 플레이리스트 24.6%
4. **정수 문턱값** — 이탈률이 5·10·40시간, skip 0.7, 일시정지 3회, 나이 25·35·61세에서만 계단식으로 꺾이고 구간 내부는 완전히 평평
5. **계수 복원** — 7개 신호를 구간 더미로 로지스틱 회귀 적합 시 계수가 거의 정확히 0.5 단위로 떨어짐

### 복원된 생성 규칙

`churned ~ Bernoulli(sigmoid(가중합))`, 절편 6.0 기준:

| 항목 | 계수 | | 항목 | 계수 |
|---|---:|---|---|---:|
| Free | +4.5 | | 청취 5–10h | −2.5 |
| skip > 0.7 | +2.5 | | 청취 10–40h | −4.5 |
| 일시정지 ≥ 3 | +2.5 | | 청취 > 40h | −7.0 |
| Student | +2.0 | | 상담 Medium | −2.5 |
| 나이 25–34 | −1.0 | | 상담 Low | −4.5 |
| 알림클릭 ≥ 5 | −1.5 | | 나이 35–60 | −2.5 |

전체 검증 과정: [`scripts/eda_insight.py`](../scripts/eda_insight.py) → [INSIGHT_REPORT.md](../artifacts/eda_insight/INSIGHT_REPORT.md) §5 한계 ①

## 실제 적용 한계

1. **성능 수치를 실제 서비스 성능으로 발표할 수 없다.** Test PR-AUC 0.947은 심어진 생성 공식을 되찾은 값이며, 실무 이탈 예측 벤치마크는 통상 0.3~0.6대다.
2. **이론 상한이 존재한다.** Bernoulli 난수 때문에 달성 가능한 최대 정확도가 86.1%이고 현행 모델이 이미 84.8%다. 남은 1.3%p는 어떤 알고리즘으로도 회수할 수 없다.
3. **예측 시점이 정의되지 않았다.** 관측 기준일·해지일이 없어 "향후 30일 내 해지" 같은 시간 지평을 주장할 수 없다.
4. **2차 행동 분석 금지.** 논리 위반 레코드가 25~30%이므로 청취 패턴·소셜 그래프 해석에 사용해서는 안 된다.
5. **인과 해석 금지.** 모든 관계는 생성 규칙의 입력 변수를 되읽은 연관성이며, 캠페인 효과는 A/B 테스트로만 확정된다.
